{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "chmod +x ./gradlew && ./gradlew bootJar --no-daemon"
  },
  "deploy": {
    "startCommand": "java -jar build/libs/*.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
